name: Build Raptor OS Security Edition ISO

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-iso:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Check Runner Disk Space
        run: |
          echo "=== Host Runner Disk Status ==="
          df -h

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            live-build \
            debootstrap \
            debian-archive-keyring \
            syslinux \
            syslinux-utils \
            isolinux \
            mtools \
            dosfstools \
            squashfs-tools \
            xorriso \
            grub-pc-bin \
            grub-efi-amd64-bin \
            po4a

      - name: Ensure Executable Script Permissions
        run: |
          chmod -R +x auto/ || true

      - name: Clean Previous Builds
        run: |
          sudo lb clean --purge || true
          sudo rm -rf .build config/binary config/bootstrap config/chroot config/common config/source

      - name: Run Live-Build Configuration
        run: |
          sudo lb config

      - name: Build Raptor OS Security Edition ISO
        run: |
          sudo DEBIAN_FRONTEND=noninteractive lb build 2>&1 | tee build.log

      - name: Verify ISO Artifact
        id: verify
        run: |
          ISO_FILE=$(find . -maxdepth 1 -type f -name "*.iso" | head -n 1)
          if [ -z "$ISO_FILE" ]; then
            echo "ERROR: No ISO file generated!"
            exit 1
          fi
          
          SIZE=$(stat -c%s "$ISO_FILE")
          if [ "$SIZE" -le 104857600 ]; then
            echo "ERROR: Generated ISO is under 100MB ($SIZE bytes). Build failed."
            exit 1
          fi

          echo "ISO_PATH=$ISO_FILE" >> $GITHUB_ENV
          echo "ISO_NAME=$(basename $ISO_FILE)" >> $GITHUB_ENV
          echo "=== ISO SUCCESSFUL BUILD ==="
          echo "File: $ISO_FILE"
          echo "Size: $(du -h "$ISO_FILE" | cut -f1)"

      - name: Upload ISO Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ISO_NAME }}
          path: ${{ env.ISO_PATH }}
          if-no-files-found: error
          retention-days: 7
