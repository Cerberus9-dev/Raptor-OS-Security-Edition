# OSINT — spec section 16. Legitimate public-source research tooling only,
# per the spec's own instruction to avoid questionable/malicious services.

whois
recon-ng

# theharvester removed — not a Debian package at all (Kali packages it
# themselves; upstream install method for Debian is a Python venv via
# uv/pip from GitHub). Moved to the best-effort installer in
# 0300-raptor-thirdparty-tools.hook.chroot instead of left here as a
# build-breaking entry.

# exiftool intentionally not repeated here — already installed via
# raptor-forensics.list.chroot (its home category: metadata inspection).
# Duplicate package names across .list.chroot files aren't a build error
# (apt just dedupes), but no reason to list it twice.
