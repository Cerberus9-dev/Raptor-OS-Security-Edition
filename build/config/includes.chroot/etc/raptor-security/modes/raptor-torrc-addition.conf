# Raptor Security additions to Tor's default config.
# Enables the control port raptor-tor-managerd needs for NewIdentity().
# Cookie auth (not a fixed password) so no secret needs to be baked into
# the image or the daemon's source.
ControlPort 9051
CookieAuthentication 1
CookieAuthFileGroupReadable 1
